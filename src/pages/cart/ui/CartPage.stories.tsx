import { Meta, StoryObj } from '@storybook/react'
import CartPage from './CartPage'

export default {
    title: 'pages/CartPage',
    component: CartPage,
} satisfies Meta<typeof CartPage>

export const Default: StoryObj<typeof CartPage> = {}
